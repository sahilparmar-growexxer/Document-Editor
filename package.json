{
  "name": "blocknote",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix api run dev",
    "dev:web": "npm --prefix web run dev",
    "dev:api": "npm --prefix api run dev"
  }
}
